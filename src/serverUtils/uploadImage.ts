/**
 * TODO: This function should upload the image to the docker
 *
 * @returns a promise that resolves to a success string from
 * the docker upload image command
 */
export async function uploadImage(): Promise<string> {
  // TODO: Replace this with the actual docker upload image command
  const result = "docker upload image result";
  const resultString = result.toString();

  return resultString;
}
